# King Express Bus — Frontend (Next.js). Placeholders only; never commit secrets.

NEXT_PUBLIC_API_URL=http://localhost:8000
# Public site origin for canonical / Open Graph / sitemap (no trailing slash).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
